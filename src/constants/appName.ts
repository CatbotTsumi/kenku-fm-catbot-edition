/** User-visible app name (window title, HTML title, alt text). */
export const APP_DISPLAY_NAME = "Kenku FM: Catbot Edition";

/** Electron productName / installer paths — no colon (invalid on Windows). */
export const APP_PRODUCT_NAME = "Kenku FM Catbot Edition";
